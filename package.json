{
  "name": "rais-website",
  "version": "1.1.0",
  "description": "Strategic update for RAIS website",
  "main": "index.html",
  "scripts": {
    "config": "node scripts/generate-public-config.mjs",
    "deploy": "npm run config",
    "predev": "npm run config",
    "prebuild": "npm run config",
    "dev": "vite",
    "build": "vite build && node scripts/postbuild-copy.mjs",
    "preview": "vite preview",
    "build:tailwind": "tailwindcss -i ./styles/tailwind.input.css -o ./styles/tailwind.generated.css --config ./tailwind.config.js --minify",
    "test:roi-webhook": "node scripts/roi-lead-smoke-test.mjs",
    "test:lead-magnet-webhook": "node scripts/test-lead-magnet-webhook.mjs"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@sentry/browser": "^10.51.0",
    "@supabase/supabase-js": "^2.100.1"
  }
}
